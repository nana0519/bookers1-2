<!DOCTYPE html>
<html>
  <head>
    <title>Bookers12</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% flash.each do |type, message| %>
      <div class="text-success">
        <%= message %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
